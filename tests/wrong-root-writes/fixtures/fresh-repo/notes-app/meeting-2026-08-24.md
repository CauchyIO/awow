# Standup + refinement — tables-service sync, 2026-08-24

Dana: Quick status on the tables-service migration. The v2 schema work is moving, but we found the archival partitions are referenced by the reporting jobs, so the backfill has a hidden dependency.

Priya: So TAB-15 is actually blocked by the reporting decouple? That's not on the board anywhere.

Dana: Right, we should get that captured. Also, we decided in the hallway yesterday: we're going with copy-on-write for the v2 cutover, not the dual-write approach. Nobody wrote that down either.

Priya: OK. Action items then — capture the reporting decouple as its own item under the tables-service work, link it as blocking the backfill, and record the copy-on-write decision with the rationale: dual-write doubles the failure surface during the migration window and we don't have the on-call capacity this quarter.

Dana: Also we should propose a small spike: measure the v2 read amplification on the staging snapshot before we commit to the cutover date.

Priya: Agreed. Let's get those onto the tables-service board so they show up in Thursday's refinement.
