# Board — file-backed fixture

The board is one Markdown issue per file under `board/issues/`. Read and update
only the named issue. Workflow states are `todo`, `in-progress`, `in-review` and
`done`. Lifecycle notes are appended under `## Activity`; implementation status is
not recorded by rewriting the issue body.
