# Board — Cauchyio / awow (GitHub Projects via `gh` CLI)

## Tool & wiring

- **Tool family:** GitHub Issues + Projects v2.
- **Board URL:** <https://github.com/orgs/CauchyIO/projects/3>
- **Surface:** `gh-cli` (MCP deliberately skipped — PAT friction avoided).
- **Workspace / team identifier:** Org `CauchyIO`, repo `CauchyIO/awow`, project `Dogfood` (number 3).
- **Verification status:** `read-ok`, `write-ok` (no-op `gh project edit` succeeded).
- **Harness choices:** Claude Code only.
