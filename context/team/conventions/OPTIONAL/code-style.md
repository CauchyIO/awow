# code-style

# OPTIONAL — defer

This convention is not required to start working with the agent. It appears as a stub so the directory tree reflects the full shape. Fill it in when the team encounters a problem that this convention would solve — typically after the first Seed cycle.

Until then, leave this file as-is. `/setup-awow` will not ask about it.
