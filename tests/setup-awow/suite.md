---
command: setup-awow
---

# Suite — setup-awow

Regression suite for the `/setup-awow` wizard. `command:` names the prompt
under `.agents/commands/` that `/test-awow` executes against each scenario's
scratch workspace. Layout, scenarios, and fixture conventions: [README.md](README.md).
